import { Routes } from '@angular/router';
import { CustomerDeviceScreenComponent } from './screens/customer-device-screen.component';

export const routes: Routes = [
  {
    path: '',
    component: CustomerDeviceScreenComponent,
  },
];
